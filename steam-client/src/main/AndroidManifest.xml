<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Steam protocol wrapper module — no Android components. INTERNET
         permission is declared in :app (where it actually takes effect). -->
</manifest>
