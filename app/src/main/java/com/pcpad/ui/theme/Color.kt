package com.pcpad.ui.theme

import androidx.compose.ui.graphics.Color

// ── App background ────────────────────────────────────────────────────────────
val DarkBackground = Color(0xFF050505)

// ── Key / button ──────────────────────────────────────────────────────────────
val KeyFill    = Color(0xFF121212)  // button container fill
val KeyOutline = Color(0xFF1F1F1F)  // button border
val KeyLabel   = Color(0xFFE6E6E6)  // button text

// ── Tab bar ───────────────────────────────────────────────────────────────────
val TabAccent  = Color(0xFFA1D9FF)  // selected tab text + indicator

// ── Misc ──────────────────────────────────────────────────────────────────────
val DarkSurface = Color(0xFF1E1E1E)
val OnAccent    = Color(0xFFFFFFFF)
