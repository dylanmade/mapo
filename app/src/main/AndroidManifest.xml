<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.INJECT_EVENTS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Steam Input config browse/import (Phase 8). The :steam-client module
         opens a WebSocket connection to Steam's CMs for Steam Guard QR login
         and (later) IPublishedFile.QueryFiles + CDN downloads. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Visibility into launchable activities so the app-picker in Auto-Switch
         can enumerate installed games / apps on Android 11+ (package-visibility
         restrictions). Narrowest scope that satisfies the use case — we don't
         need QUERY_ALL_PACKAGES. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".MapoApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mapo">

        <!-- Shizuku provider. The Shizuku Manager app talks to us via this
             ContentProvider to deliver the binder + permission grant results.
             Required even when Shizuku Manager isn't installed (then it just
             never gets queried). See rikka-apps/Shizuku-API docs. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Mapo.Translucent"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground host for the live overlay editor: hosts the frozen backdrop. Exits: toolbar
             Done, back (→ confirm), and Home (onUserLeaveHint). Recents/overview does NOT exit —
             the session stays intact. Own task (taskAffinity="") so it stays foreground while
             editing; excludeFromRecents + singleTask: a transient editing surface. -->
        <activity
            android:name=".ui.overlay.OverlayEditActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/Theme.Mapo.Translucent"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <service
            android:name=".service.InputAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Foreground service that holds process priority while the rebuilt button
             overlay (run mode or live edit) is mounted. Named "keyboard" for historical
             reasons; now shared infra for the element overlay. -->
        <service
            android:name=".service.overlay.keyboard.KeyboardOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="button_overlay" />
        </service>

    </application>

</manifest>
